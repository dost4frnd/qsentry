model: qlstm
params:
  hidden_size: 8
  n_qubits: 4
  n_layers: 1
  diff_method: parameter-shift   # faithful to the manuscript; use 'backprop'/'adjoint' to iterate faster
train:
  epochs: 30
  lr: 0.01
  batch_size: 32
  weight_decay: 0.00001
  patience: 6
